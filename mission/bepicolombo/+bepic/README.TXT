MATLAB root package for BepiColombo MATLAB code.

If the irfu-matlab/mission/bepicolombo directory is added to the MATLAB path
(typically by executing irf()), then MATLAB code placed directly under this
directory will be accessible as bepic.* in MATLAB, e.g. plot_something.m is
accessible as bepic.plot_something.
