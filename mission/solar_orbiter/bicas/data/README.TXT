This directory should contain master CDF files for the datasets which BICAS produces.
These files might not be included in the irfu-matlab git repository where BICAS is developed.

This file also exists to force git to include this directory in the irfu-matlab git repository, since git can not include empty directories.
